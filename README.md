## This is a simple bash script that moves all the non files from your desktop  to the trash
only works with Mac since it uses trash command

## From terminal
cd into the folder  then bash trash_desktop_files.sh

it can easily change to work with files as well
